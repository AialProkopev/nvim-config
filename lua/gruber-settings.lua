local gruber = require("gruber-darker.config")

gruber.setup({
      bold = false,
      italic = {
        strings = false,
        comments = false,
        operators = false,
        folds = false,
      },
})
